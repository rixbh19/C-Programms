#include<stdio.h>
void main()
{
    int a,b;
    printf("Enter Two Number : ");
    scanf("%d%d",&a,&b);
    if (a>b) 
    {printf("%d is the greatest.",a);}
    else
    {printf("%d is the greatest.",b);}
}